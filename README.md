# cloobster-barcode-service

Very simple REST service using barcode4j library.

URL-TO-SERVICE/{type}/{code}

Generates barcode with given type and encodes given code.
Currently only type 39 is supported.
